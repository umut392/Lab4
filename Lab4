dictionary: dict[str,list]={}
items={}
numOfUsers=input("enter number of users : ")
print(numOfUsers)


for x in range(int(numOfUsers)):
    userName=input("enter user name : ")
    numOfItmes=input("enter number of items : ")
    array=[]
    for y in range(int(numOfItmes)):
        item=input("enter item name : ")
        array.append(item)
        if item in items:
            value=items.get(item)
            value=value+1
            items.update({item:value})
        else: 
            items.update({item:1})
    dictionary.update({userName:array})

print()
print("TASK STRUCTURE ")
for x in dictionary:
    print(x,"-->",dictionary.get(x))

print()
print("initial tasks (no dependencies)")
initalteskvarmi=False
for x in dictionary:
    if len(dictionary.get(x))==0:
        print(x)
        initalteskvarmi=True

if not initalteskvarmi:
    print("No task can be started")
    
    
if initalteskvarmi:
    print("executon order :")
    
    while dictionary:
        aa=True 
        a=1
        for gezenkey in dictionary:
            if len(dictionary.get(gezenkey))==0:# length i 0 olanı bulduk
                print("step ",a,": ",gezenkey)
                a+=1
                aa=False
                silinecek=gezenkey
                for gezen2 in dictionary:# her keyin arrayinde bu eleman varsa sil
                    if silinecek in dictionary.get(gezen2):
                        dictionary.get(gezen2).remove(silinecek)
            #burda da dictionary den key olarak silicez
            #del sozluk["yas"]
                del dictionary[gezenkey]
                break
        print(dictionary)
        if aa:
            break
    if dictionary:
        print("ERROR: Circular dependency detected! These tasks could not be completed:")
        for a in dictionary:
            print(a)

    


        
    
        
